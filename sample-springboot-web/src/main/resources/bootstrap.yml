server:
  port: 8080
#日志打印
logging:
  level:
    org.springfromework.web: debug

spring:
  application:
    name: my-web
  cloud:
    config:  # git管理配置
      uri: http://127.0.0.1:6018
      label: master
      profile: test

management:
  security:
    enabled: false #在 application.properties 添加以下配置.关闭安全认证 值是false的话，除开health接口还依赖endpoints.health.sensitive的配置外，其他接口都不需要输入用户名和密码了



