server:
  port: 8080

spring:
  application:
    name: my-web
  cloud:
    config:  # git管理配置
      uri: http://111.231.83.73:6018
      label: master
      profile: dev
#      discovery:
#        enabled: true #是从配置中心读取文件。
#        service-id: my-config #配置中心的servieId，即服务名。





